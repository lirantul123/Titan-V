apiVersion: v1
kind: Service
metadata:
  name: {{ include "titan-v-app.fullname" . }}
  labels:
    {{- include "titan-v-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "titan-v-app.selectorLabels" . | nindent 4 }}
