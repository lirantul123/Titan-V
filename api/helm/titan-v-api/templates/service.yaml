apiVersion: v1
kind: Service
metadata:
  name: {{ include "titan-v-api.fullname" . }}
  labels:
    {{- include "titan-v-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "titan-v-api.selectorLabels" . | nindent 4 }}
